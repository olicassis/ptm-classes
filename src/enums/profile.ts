export enum ProfileStatus {
  VERIFIED = 'verified',
  UNVERIFIED = 'unverified',
}

export enum ProfileRole {
  STUDENT = 'student',
  TEACHER = 'teacher',
}

export enum ProfileScheduleStatus {
  AVAILABLE = 'available',
  UNAVAILABLE = 'unavailable',
}
