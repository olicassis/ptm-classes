{
  "name": "@ptm/typescript-config",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint-staged": "lint-staged"
  },
  "lint-staged": {
    "*.ts": [
      "eslint '{src,tests}/**/*.{js,ts,tsx}' --fix"
    ],
    "*.json": [
      "prettier --write"
    ]
  }
}
